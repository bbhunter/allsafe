<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/terminal_border"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MISSION BRIEFING:"
        android:textColor="@color/secondaryColor"
        android:textSize="12sp"
        android:textStyle="bold"
        android:fontFamily="monospace"
        android:letterSpacing="0.1" />

    <TextView
        android:id="@+id/briefing_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="This application stores sensitive user credentials using SharedPreferences, a standard Android storage mechanism. Your objective is to extract the stored data using mobile penetration testing techniques."
        android:textColor="@color/white"
        android:textSize="13sp"
        android:fontFamily="monospace"
        android:lineSpacingExtra="2dp" />

    <TextView
        android:id="@+id/briefing_target"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="TARGET: Retrieve the flag hidden in SharedPreferences storage"
        android:textColor="@color/difficultyMedium"
        android:textSize="12sp"
        android:textStyle="normal"
        android:fontFamily="monospace" />

</LinearLayout>
